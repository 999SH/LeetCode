/**
 * @param {string} s
 * @return {boolean}
 */
//()[]{}
const isValid = function(s) {
    let stack = []
    for (let i = 0; i < s.length; i++) {
        stack.push(s[i])
    }
    while(stack){

    }
};

console.log(isValid("([][][][])"))